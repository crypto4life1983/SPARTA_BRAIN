# Decisions

- 2026-09-09 — **Rebuilt the three lost modules and committed them** (`spartacus/agents/amazon_engine.py`, `spartacus/product_lock.py`, `spartacus/agents/product_locker.py`; commit d8d7a767, 29 new tests). Same rules as before: real catalog only, verbatim registered URLs only, CTA-only fallback, strict/pending/discovery lock states, link-guarded LLM text with deterministic fallback. Restored the one documented registry entry (Desk Cable Organizer → amazon.ca URL, verbatim from 2026-04-28 notes). `config/affiliate_links.json` (product lock) was NOT recreated: no lock, discovery mode, until the operator sets one via /api/product-lock.

- 2026-09-15 — **Income engines switched back ON (operator: "ok do it").** Settings now: `amazon_autopilot_enabled=true`, `autopilot_enabled=true`, and **`auto_upload_youtube` deliberately left FALSE** — per operating_rules.md ("For YouTube: review/approval before public upload unless explicitly allowed"), generation is on but nothing publishes. Verified `_maybe_auto_upload()` short-circuits on that flag (returns "auto_upload_youtube=off") before any uploader import, so the autopilot produces local artifacts only. `safety_gate_enabled=true` kept on. Root cause of the 4-month outage confirmed from the autopilot log: the 2026-06-04 run crashed with `ImportError: cannot import name 'product_locker'` — the same missing module rebuilt on 2026-09-11; every scheduled run since had been dying there. Proof of life today: `/api/amazon/generate` → 2026-09-15 brief with 3 real catalog products (Rode PodMic USB, Dymo LabelWriter 550, Elgato Key Light Air), and `/api/shorts/generate` → 4 angles off that brief. Both correctly in **CTA-only mode** (`any_cta_only_mode=true`, `lock_state=discovery`) because only one product has a registered affiliate URL — the engines refuse to invent links, as designed. **Operator action required: register real affiliate URLs** (`POST /api/amazon/affiliate-links`, or the /amazon panel) for the products to be pushed; until then every idea ships without a link and earns nothing. Publishing stays a separate, explicit decision.
