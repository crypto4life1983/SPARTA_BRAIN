# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Project DB + exports
sparta.db
sparta.db-journal
exports/*.txt

# OS + IDE
.DS_Store
.idea/
.vscode/
.claude/

# Secrets + env
.env
.env.*

# Local backups
*.bak

# Runtime + generated output trees
logs/
outputs/
storage/
logsserver.log

# Daily Journal Snapshot exports (generated by the scheduler)
reports/journal_snapshot_*.json
reports/journal_snapshot_*.md

# Rendered media + temp media artifacts
*.mp4
*.wav
*.tmp

# JS deps (if ever introduced)
node_modules/

# Local secrets + local-only config / manual inputs (never commit)
local_secrets/
config/
manual_inputs/

# Strategy Factory routine layer — generated runtime outputs (regenerable;
# README is kept trackable)
reports/strategy_factory_routines/daily_state/
reports/strategy_factory_routines/strategy_queue/
reports/strategy_factory_routines/weekly_review/
reports/strategy_factory_routines/next_bundle/
reports/strategy_factory_routines/candidate_registry/

# Autopilot morning report — generated runtime status surface (regenerable)
reports/autopilot_morning/

# Candidate #11 cross-asset dispersion reversion — gitignored detector labels /
# replay artifacts (real-candle outputs; SHA-pinned in the review contracts)
data/cross_asset_dispersion_reversion_c11/

# Candidate #12 failed-breakdown reclaim reversal — gitignored detector labels /
# replay artifacts (real-candle outputs; SHA-pinned in the review contracts)
data/failed_breakdown_reclaim_reversal_c12/

# Candidate #13 lead-lag propagation continuation — gitignored detector labels /
# replay artifacts (real-candle outputs; SHA-pinned in the review contracts)
data/lead_lag_propagation_continuation_c13/

# Candidate #14 conviction-bar follow-through — gitignored detector labels /
# replay artifacts (real-candle outputs; SHA-pinned in the review contracts)
data/conviction_bar_follow_through_c14/

# Candidate #15 slow vol-targeted time-series momentum — gitignored detector
# labels / replay artifacts (real-candle outputs; SHA-pinned in review contracts)
data/slow_vol_targeted_time_series_momentum_c15/

# Candidate #16 cointegration pairs market-neutral — gitignored detector labels /
# replay artifacts (real-candle outputs; SHA-pinned in the review contracts)
data/cointegration_pairs_market_neutral_c16/

data/risk_adjusted_portfolio_construction_vol_targeted_allocation_c17/
